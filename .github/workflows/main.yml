on: push
name: 🚀 Deploy run on push
jobs:
   web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    - name: Clean install dependencies
      run: |
        npm ci
    - name: Build application
      run: |
        npm run build